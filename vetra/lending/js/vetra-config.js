/* ================================================================
   Ключи от базы Ветры (Supabase).
   Оба значения — публичные, их видно в коде страницы: так и задумано.
   Защита данных живёт в самой базе (правила RLS в db/schema.sql).

   Где взять:  supabase.com → ваш проект → Settings → API
     Project URL  → SUPABASE_URL
     anon public  → SUPABASE_ANON_KEY

   НИКОГДА не вставляйте сюда ключ service_role — он даёт полный доступ.
   ================================================================ */
window.VETRA_CONFIG = {
  SUPABASE_URL:      'СЮДА_URL_ПРОЕКТА',
  SUPABASE_ANON_KEY: 'СЮДА_КЛЮЧ_ANON'
};
